{
  "name": "my-klikly-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --parallel dev",
    "dev:web": "pnpm --filter @klikly/web dev",
    "dev:api": "pnpm --filter @klikly/api dev",
    "build": "pnpm --filter @klikly/web build",
    "build:all": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "pg": "^8.21.0",
    "pnpm": "^10.0.0",
    "postgres": "^3.4.9"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "packageManager": "pnpm@10.6.3",
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  }
}
